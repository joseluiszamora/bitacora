import 'package:flutter/material.dart';

/// Paleta de colores de la aplicación BITACORA.
class AppColors {
  AppColors._();

  // Azules principales
  static const Color primary = Color(0xFF0A3D62);
  static const Color primaryLight = Color(0xFF1E5A9E);
  static const Color primaryAccent = Color(0xFF234FBA);

  // Dorados
  static const Color gold = Color(0xFFD4AF37);
  static const Color goldSecondary = Color(0xFFEFBF04);

  // Grises neutros
  static const Color grey = Color(0xFF475569);
  static const Color greyDark = Color(0xFF334155);

  // Fondos
  static const Color backgroundLight = Color(0xFFF8FAFC);
  static const Color backgroundDark = Color(0xFF0F172A);

  // Blancos
  static const Color white = Color(0xFFFFFFFF);
  static const Color offWhite = Color(0xFFF1F5F9);

  // Estado
  static const Color success = Color(0xFF15803D);
  static const Color error = Color(0xFFDC2626);
  static const Color warning = Color(0xFFF59E0B);
}
